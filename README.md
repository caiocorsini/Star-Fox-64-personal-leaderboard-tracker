Star Fox 64 Record Recorder

Run the GUI with:

```bash
python recorder.py
```

Place `Star Fox 64 - All Possible Routes - Sheet1.csv` in the same folder (already present).

The app saves leaderboards to `sf64_records.json` in the same folder. Select a path and difficulty, enter a player name (optional) and a numeric score, then click "Add Score".
